# Embedded

A local-first engine for Convex — run plain Convex queries and mutations on-device, against local storage backed by turso

## Develop

```bash
vp install   # install dependencies
vp check     # format, lint, typecheck
vp test      # run tests
```

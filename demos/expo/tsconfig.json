{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "allowImportingTsExtensions": true,
    "paths": {
      "@/*": ["./*"],
      "$convex/*": ["../../convex/*"],
      "$local/*": ["../../local/*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "../../convex/**/*.ts",
    "../../convex/**/*.d.ts",
    "../../local/**/*.ts"
  ]
}
